{
  "name": "checkbox-task-manager",
  "version": "1.0.0",
  "scripts": {
    "docker:build": "docker-compose up --build -d",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  }
}
